<!-- 补建说明：该目录说明为后续补建，用于记录 TUI 验证 fixture 的归属；当前进度：Task 7 首版，fixtures 通过 goldens 与 corpus manifest 引用，避免复制大段语料。 -->
# Fixtures

Fixtures here are small, reviewable validation inputs. Large text samples should be referenced from `corpora/*.txt` instead of copied.
