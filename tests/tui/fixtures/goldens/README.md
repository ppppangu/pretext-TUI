<!-- 补建说明：该目录说明为后续补建，用于记录 TUI golden fixture 的格式与用途；当前进度：Task 7 首版，主 golden 存放在 accuracy/tui-reference.json。 -->
# Goldens

Golden cases use machine-readable JSON with a top-level `metadata` object. They should stay compact enough for code review and should cover terminal semantics rather than browser snapshots.
