<!-- 补建说明：该目录说明为后续补建，用于记录 TUI fuzz seed 的维护规则；当前进度：Task 7 首版，CI 只打印失败复现，人工最小化后再提交 seed。 -->
# Fuzz Seeds

Checked-in seeds should be minimized reproductions of real failures. The fuzz script does not automatically write CI failures into this directory.
