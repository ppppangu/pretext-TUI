<!-- 补建说明：该目录说明为后续补建，用于记录 Task 8 的 terminal demo fixture；当前进度：首版提供 mixed-terminal-session.txt，供 package-level demo 展示 row count、resize reflow 与 visible-window materialization。 -->
# Fixtures

This directory contains small package-level fixtures for terminal demos.

Fixtures should look like realistic terminal text but must not imply a renderer, application shell, input system, persistence layer, or package-external integration.
