{
  "metadata": {
    "note": "补建说明：该文件为后续补建，用于记录 TUI validation gate 的机器可读状态；当前进度：Phase 9 已将 release gate 对齐 memory budget 与 benchmark evidence guard，review 结论为 approve with documented residual risk。",
    "schema": "pretext-tui-status@1"
  },
  "gate": {
    "check": "required",
    "typecheck:tui": "required",
    "typecheck:tui-validation": "required",
    "tui-static-gate": "required",
    "oxlint --type-aware src": "required via check",
    "test:tui": "required",
    "tui-oracle-check": "required",
    "tui-corpus-check": "required",
    "tui-fuzz --seed=ci --cases=2000": "required",
    "benchmark-check:tui": "required",
    "memory-budget-check:tui": "required",
    "terminal-demo-check": "required",
    "api-snapshot-check": "required",
    "package-smoke-test": "required"
  },
  "scope": {
    "browserProductSurface": "removed from active validation",
    "hostAppIntegration": "out of scope",
    "tarballRootDist": "public wrappers only; implementation modules are under dist/internal",
    "virtualTextPrimitives": "Task 9 sparse index, page cache, source lookup, incubating layout bundle, and append invalidation landed",
    "genericRangeSidecar": "Phase 4 incubating generic source range index landed; metadata remains inert and host-owned",
    "sourceFirstSearchSessions": "Phase 5 incubating source-first search sessions landed; hits are source ranges with optional projection and no UI state",
    "selectionExtraction": "Phase 6 incubating selection/extraction helpers landed; results are immutable source-first data with no clipboard or active selection state",
    "richMetadataHardening": "Phase 7 internal rich span/raw-visible indexes, rich materialize option validation, and benchmark schema guardrails landed without expanding public exports",
    "appendOnlyChunkedStorage": "Phase 8 append-only chunked storage landed behind existing cell-flow/bundle boundaries with documented residual risk",
    "performanceMemoryEvidence": "Phase 9 approved with documented residual risk: benchmark counters, modelled memory budgets, release gate consistency evidence, and claim guardrails are implemented"
  }
}
