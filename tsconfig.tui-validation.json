// 补建说明：该配置为后续补建，用于 typecheck TUI validation scripts/tests；当前进度：Task 8 覆盖 terminal-demo、scripts/tui-*.ts 与 tests/tui/**/*.ts，不引入 DOM lib。
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "lib": ["ESNext"],
    "types": ["bun", "node"],
    "allowJs": false,
    "noEmit": true
  },
  "include": [
    "scripts/terminal-demo.ts",
    "scripts/terminal-demo-check.ts",
    "scripts/tui-*.ts",
    "tests/tui/**/*.ts"
  ],
  "exclude": [
    "dist",
    "node_modules"
  ]
}
